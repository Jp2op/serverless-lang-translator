import React from "react";
import ReactDOM from "react-dom/client";
import SimpleTranscriber from "./SimpleTranscriber";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <SimpleTranscriber />
  </React.StrictMode>
);